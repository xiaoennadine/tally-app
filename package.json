{
  "name": "tally-app",
  "version": "1.0.0",
  "description": "Tally — Splitwise-style expense tracker. Telegram bot + Mini App. Vercel + KV.",
  "scripts": {
    "dev": "vercel dev",
    "start": "node local-server.js"
  },
  "dependencies": {
    "@vercel/kv": "^2.0.0",
    "node-telegram-bot-api": "^0.66.0"
  },
  "engines": {
    "node": ">=18"
  }
}
